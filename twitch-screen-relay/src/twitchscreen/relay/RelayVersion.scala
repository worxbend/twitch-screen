package twitchscreen.relay

/** The version reported by `GET /api/v1/status` and shown in the OpenAPI document. Bump it here and nowhere else. */
private[relay] object RelayVersion:
  val current: String = "0.1.0"
