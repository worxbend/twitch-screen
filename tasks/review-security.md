# Review security and Twitch remediation

Baseline: `a66a052` (implemented atop the orchestration commit). Work was groomed against the current OAuth implementation, implemented in an isolated worktree, and independently reviewed by the firmware/protocol agent. Root owns deployment integration and the final push.

| Finding | Grooming disposition | Implementation / evidence |
|---|---|---|
| RLY-01,05,06 | Confirmed: public management, wildcard CORS, unbounded bodies | Fixed: fail-closed Basic/Bearer access, explicit public routes, pre-buffer 64KiB body limit and 128 HTTP connections. Password derivation uses the compute pool with two nonblocking permits per server. Real Netty tests cover declared/chunked limits, all 16 protected operations, exact callbacks, public health/stats/docs, per-operation OpenAPI OR alternatives, token rotation and cross-site Basic rejection. Notification field limits are in the runtime slice. |
| RLY-02 | Token selection fixed by OAuth commit; registration reporting remained open | Fixed remaining gap: no default user credential on the client; webhook reconciliation passes the app-token fallback explicitly, observes actual registration status and retries unusable/missing subscriptions. Scripted Helix registration fails once then recovers. |
| RLY-03 | Confirmed permanent startup failure and stale health | Fixed: supervised startup retries with delays capped at 60 seconds, scoped client rebuild on WebSocket subscription failure, 30-second reconciliation, and shared component health. An actor orders health changes and bus publication; concurrency test checks final link event against status. |
| RLY-10 | OAuth already refreshes; expired/rejected tokens and transition races remained | Fixed: expired/rejected access tokens are withheld; actual Twitch4J UnauthorizedException under Hystrix triggers refresh and stops repeated scoped failures. Actor-owned accepted transitions serialize token, credential handle and disk changes. Tests cover stale remote refresh completion racing sign-out/new consent; actor-boundary inspection establishes serialization of post-CAS side effects. |
| RLY-11 | Confirmed missing grant/scopes and ignored WebSocket outcomes | Fixed: validate broadcaster grant and scopes, inspect register outcomes, track each subscription's health, and rebuild the whole scoped client after failure or access-token change. Sticky rebuild requests survive unrelated successes; this also restores subscriptions Twitch4J discarded permanently. Missing consent stays degraded while listeners and available integrations run. |
| RLY-43 | Confirmed ingestion before callback binding | Fixed: construct endpoints and device listener, bind HTTP, then call startIngestion. A real local callback request from the after-bind hook proves reachability before ingestion starts. Existing asynchronous token maintenance can precede binding. |
| RLY-04 | Syntax fixed before this work | Root owns image build/smoke |
| RLY-08 | Confirmed conflicting push/poll observations | Fixed: pushed transitions take precedence for 60 seconds and offline polls require two consecutive absences. Actor-owned state changes publish in the same actor turn, preventing concurrent START/END reordering. Contradictory-source and blocked-publication regression tests pass. |
| RLY-19 | Confirmed duplicate authenticated deliveries | Fixed: bounded atomic delivery-ID claims; full cache returns 503 without evicting live IDs. Retain IDs through signed timestamp plus ten minutes, including the exact boundary, so accepted future-skew deliveries cannot replay after arrival-based expiry. Concurrent duplicate and endpoint freshness/expiry tests pass. |
| RLY-20,21 | Confirmed stale webhook metadata and nullable provider text | Fixed: webhook channel updates feed the same tracker; nullable title/category become empty text. Tests verify metadata update followed by online and null normalization. |
| RLY-23,24,25,26,48 | Confirmed provider/config validation gaps | Fixed: provider URL/secret requirements, positive timers, representable wire seconds/TTL, greeting queue capacity, fixed TSB/3 frame/version constraints, and path-qualified reader failures. Basic credentials reject partial/blank/unencodable user IDs; Bearer secrets require a header-safe ASCII alphabet. ConfigSuite covers boundaries. |
| RLY-35,56 | Confirmed startup timestamp and duplicated rule conversion | Fixed: Main captures startup once before config/dependency setup and injects it into StatusApi; actual HTTP test verifies 300 seconds of startup-inclusive uptime. Dependencies computes alert rules once and passes the list to AlertMonitor and API assembly. |
| RLY-42 | Current first-live announcement explicitly specified in tests and initializes statistics | Intentional behavior retained: the first live observation emits START and initializes stream counters; the first offline observation is silent. Both paths are tested. Changing to a silent live baseline would require a distinct counter-initialization event. |
| RLY-49 | Existing Twitch secrets correctly masked; auth adds secret fields | Verified: all current secrets, management verifier and API token are absent from config, logs and activity export. Real-route fixture invokes production Config.log before asserting redaction. |
| RLY-12,37 | Shared diagnostics coverage delegated across agents | Added real JSON 400/401/403/404/413/500 assertions, fixed-clock uptime and Twitch status checks; runtime agent owns remaining diagnostics. |
| RLY-53 | Source report contradicted by pinned Tapir 1.13.31 defaultHandlers implementation | Refuted on current pinned dependency: real Netty regression verifies 500 application/json {"error":"Internal server error"}; private detail absent. |
| RLY-54 | Optional unused factory cleanup | Removed five factories after checking current call sites; kept the factories actually used by WebSocket/webhook subscription registration. |
| RLY-57 | Order-sensitive definitions | Converted derived mappings to methods. |
| RLY-58 | Optional API expansion with no demonstrated client requirement | Deferred: retain compatible error envelope |

### RLY-03 addendum (round 2): application-token rejection rebuilds the client

Gap: the round-1 fix retried and rebuilt only on startup failures and WebSocket subscription failures. A 401 on an application-token Helix call was only observed as Degraded, so the session kept retrying with the same client and its cached app token. twitch4j 1.27.0's `TwitchHelixErrorDecoder` raises `UnauthorizedException` but never evicts that token, so the session never recovered on its own.

Fix: `HelixPoller.pollStream` (streams, both transports) and `EventSubWebhookApi.reconcileSubscriptions` (list/delete/create, webhook) now call an `appTokenRejected` callback when `HelixPoller.isUnauthorized` matches, including Hystrix-wrapped 401s. `LiveTwitchSource.appTokenRejected` sets the session's `restartRequested` and reports `startup: application token rejected; rebuilding client`, logged once per session with no token text. `maintainSubscriptions` then exits after its 30-second pass, and the new `superviseSessions` loop closes the client and builds a fresh one. User-token 401s (followers/subscribers) still call only `auth.rejectAccessToken()`. A 401 during `resolveBroadcasterId` now throws `ApplicationTokenRejected`, which has no cause and no provider text, instead of retrying forever against the same client. `superviseSessions` catches session failures, reports them on `startup` and pauses `SessionFailurePause` (30 s), so there are no tight rebuild loops. Non-authorization failures keep today's Degraded-and-retry behavior. The health model is unchanged.

Evidence: 8 new tests in TwitchRecoverySuite (14 total) use scripted Helix proxies that throw Twitch4J's decoded 401 inside a Hystrix command: create 401 and list 401 both request a rebuild; a non-auth create failure does not; a streams 401 requests a rebuild without rejecting the user grant; a user 401 withholds the grant without a rebuild; a non-auth streams failure does not rebuild; the restart-flag seam works; and two end-to-end `superviseSessions` tests with a fake client factory check the second build, closure of both clients and the 1 s and 30 s pauses. Test commands disable the Hystrix circuit breaker so scripted failures cannot short-circuit later tests. Production is unaffected: twitch4j keys breakers per method, and these call rates (about one poll and one reconciliation pass per 30 s) are far below Hystrix's 20 requests per 10 s threshold. A mutation check that disabled both hooks failed 4 of the new tests. See [review-round2-relay.md](review-round2-relay.md) for the commands and counts.

Validation: eight focused suites pass 74 tests: ConfigSuite (19), ManagementAuthSuite (7), ManagementRoutesSuite (4), TwitchRecoverySuite (6), TwitchAuthSuite (12), EventSubWebhookSuite (13), ChannelStateTrackerSuite (11), WebhookDeduplicationSuite (2). The independent reviewer reran the same suite set successfully. Scalafmt and diff whitespace checks pass. Root performs the combined suite and Docker/SIGTERM smoke after integration; Main preserves its ApplicationLifetime shutdown wrapper.

No live Twitch credentials are available. Actual provider acceptance remains a deployment check; local tests use the pinned provider decoder, actual HTTP server, scripted Helix adapters and controlled state transitions. The management tests cover all assembled endpoints rather than substituting a single representative route.

Operator contract: `RELAY_HTTP_AUTH_BASIC_USERNAME` plus `RELAY_HTTP_AUTH_BASIC_PASSWORD_HASH`, or `RELAY_HTTP_AUTH_API_TOKEN`, or both. At least one complete valid method is mandatory. Password hashes use `pbkdf2-sha256$600000$<base64 salt>$<base64 digest>` generated by `tools/hash_management_password.py`; preserve literal dollar signs when exporting. Bearer tokens contain at least 32 bytes from the ASCII Bearer alphabet (for example `openssl rand -hex 32`). Restart to rotate static credentials. Password checks beyond two concurrent derivations return JSON 503 and may be retried.

Recovery contract: listeners bind before ingestion/subscription work; recoverable initialization retries at 1, 2, 4, 8, 16, 32, then 60 seconds. Webhook reconciliation and chat/subscription health checks run every 30 seconds. WebSocket failures or grant changes rebuild the scoped client within a maintenance cycle. Token maintenance withholds expired/rejected tokens and retries refresh; sign-out serializes local removal from memory, shared credential handle and disk before attempting provider revocation. Health reports Connecting, Connected, Degraded or Disconnected from ongoing component observations.
