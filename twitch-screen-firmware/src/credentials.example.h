// Copy this file to credentials.h and fill in real values.
// credentials.h is git-ignored and must never be committed.
#pragma once

#define WIFI_SSID "your-ssid"
#define WIFI_PASSWORD "your-password"

// Notification server, protocol v2 (persistent TCP, docs/PROTOCOL.md).
#define SERVER_HOST "192.168.1.100"
#define SERVER_PORT 8099

#define DEVICE_ID "roundlcd-01"
